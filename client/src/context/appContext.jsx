import { useReducer, createContext } from "react";

const AppReducter = (state, action) => {
    switch (action.type) {
        case 'ADD_EXPENSE':
            return

            break;
        
        default:
            break;
    }
}